{
  "name": "package.json",
  "version": "1.0.0",
  "description": "Tested with ```curl http://localhost:8000``` on terminal\r -------------------------------\r We’ve now set up a server and got our first server response.",
  "main": "hello.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
